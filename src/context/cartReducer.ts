/* const initialState = {
    products: [],
    cart: [],
} */

export const cartReducer = (state : any, action : any) => {
  switch (action.type) {
      case 'create':
          
          break;
  
      default:
          return state;
  }

    
}
